#!/bin/bash
set -euo pipefail

export RC_FILE="$HOME/.bashrc"
export SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export DOTFILES_DIR="$(dirname "$SCRIPT_DIR")"

for script in "$SCRIPT_DIR/configure.d/"*; do
    [ -f "$script" ] && bash "$script"
done
